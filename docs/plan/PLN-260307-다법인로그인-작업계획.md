# 작업계획서: 다법인 소속 사용자 로그인 시 법인 선택

- **문서번호**: PLAN-다법인로그인-작업계획-20260307
- **작성일**: 2026-03-07
- **관련 분석서**: REQ-다법인로그인-20260307

---

## 단계별 구현 계획

### 1단계: 로그인 페이지 register 링크 주석처리

**파일**: `apps/web/src/domain/auth/pages/LoginPage.tsx`
- 154-162행의 register 링크 블록 주석처리

### 2단계: EntitySelectPage 생성

**파일**: `apps/web/src/domain/auth/pages/EntitySelectPage.tsx` (신규)
- 로그인 후 다법인 사용자에게 법인 선택 화면 표시
- 법인 카드 클릭 → `useEntityStore.setCurrentEntity()` → navigate('/')
- 로그아웃 버튼 포함

### 3단계: 로그인 플로우 수정

**파일**: `apps/web/src/domain/auth/pages/LoginPage.tsx`
- `navigate('/')` → `navigate('/select-entity')` 로 변경

### 4단계: 라우팅 추가

**파일**: `apps/web/src/router/index.tsx`
- `/select-entity` 라우트 추가 (인증 필수)

### 5단계: EntitySelectPage에서 단일 법인 자동 리다이렉트

**파일**: `apps/web/src/domain/auth/pages/EntitySelectPage.tsx`
- 법인 1개면 자동 선택 후 바로 대시보드 이동
- 법인 0개면 적절한 메시지 표시

### 6단계: i18n 번역 키 추가

**파일**: `apps/web/src/locales/{en,ko,vi}/auth.json`
- selectEntity 관련 번역 키 추가

---

## 수정 파일 목록

| # | 파일 | 변경 |
|---|------|------|
| 1 | `apps/web/src/domain/auth/pages/LoginPage.tsx` | register 링크 주석, navigate 경로 변경 |
| 2 | `apps/web/src/domain/auth/pages/EntitySelectPage.tsx` | 신규 생성 |
| 3 | `apps/web/src/router/index.tsx` | `/select-entity` 라우트 추가 |
| 4 | `apps/web/src/locales/en/auth.json` | 번역 키 추가 |
| 5 | `apps/web/src/locales/ko/auth.json` | 번역 키 추가 |
| 6 | `apps/web/src/locales/vi/auth.json` | 번역 키 추가 |

---

## 사이드 임팩트

- **낮음**: 단일 법인 사용자는 EntitySelectPage에서 자동 리다이렉트되므로 기존 UX 변화 없음
- ADMIN_LEVEL 사용자는 모든 법인 접근 가능하므로 EntitySelectPage에서 전체 법인 목록 표시
- 기존 EntitySelector(헤더 드롭다운)는 유지 — 로그인 후에도 법인 전환 가능
