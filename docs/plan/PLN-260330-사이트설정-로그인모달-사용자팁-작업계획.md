# 작업 계획서: 사이트 설정 — 로그인 모달 & 사용자 팁

> **문서번호**: PLAN-사이트설정-로그인모달-사용자팁-작업계획-20260330  
> **작성일**: 2026-03-30  
> **참조**: REQ-사이트설정-로그인모달-사용자팁-20260330

---

## 1. 시스템 개발 현황 분석

| 항목 | 현황 |
|------|------|
| Entity Settings 카드 | 11개 (`ENTITY_MENU_ITEMS` 배열) |
| Entity 설정 저장 | `amb_hr_entities` (기본정보/급여), `amb_entity_ai_configs` (AI 설정) |
| 사이트 구성 테이블 | 없음 |
| 메뉴 팁 테이블 | 없음 |
| 로그인 후 모달 | 없음 (MainLayout에 Notification/AI 모달만 존재) |

---

## 2. 단계별 구현 계획

### Phase 1: DB 스키마 + Backend Entity/Service/Controller

**목표**: 사이트 설정 및 메뉴 팁 CRUD API 완성

#### 1-1. 신규 테이블

**`amb_entity_site_configs`** — Entity별 사이트 설정 (1:1)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `esc_id` | UUID PK | 설정 ID |
| `ent_id` | UUID FK | Entity ID |
| `esc_login_modal_enabled` | BOOLEAN | 로그인 모달 노출 여부 |
| `esc_login_modal_title` | VARCHAR(200) | 모달 제목 |
| `esc_login_modal_content` | TEXT | 모달 내용 |
| `esc_created_at` | TIMESTAMPTZ | 생성일 |
| `esc_updated_at` | TIMESTAMPTZ | 수정일 |

**`amb_entity_menu_tips`** — 메뉴별 팁

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `emt_id` | UUID PK | 팁 ID |
| `ent_id` | UUID FK | Entity ID |
| `emt_menu_code` | VARCHAR(50) | 메뉴 코드 |
| `emt_title` | VARCHAR(200) | 팁 제목 |
| `emt_content` | TEXT | 팁 내용 |
| `emt_is_active` | BOOLEAN | 노출 여부 |
| `emt_sort_order` | INT | 정렬 순서 |
| `emt_created_at` | TIMESTAMPTZ | 생성일 |
| `emt_updated_at` | TIMESTAMPTZ | 수정일 |

#### 1-2. Backend 파일

| 파일 | 유형 | 설명 |
|------|------|------|
| `entity-site-config.entity.ts` | Entity | amb_entity_site_configs |
| `entity-menu-tip.entity.ts` | Entity | amb_entity_menu_tips |
| `entity-site-config.service.ts` | Service | 사이트 설정 CRUD |
| `entity-site-config.controller.ts` | Controller | API 엔드포인트 |

#### 1-3. API 엔드포인트

| Method | Path | 설명 |
|--------|------|------|
| `GET` | `/entity-settings/site-config` | 사이트 설정 조회 |
| `PUT` | `/entity-settings/site-config` | 사이트 설정 저장 |
| `GET` | `/entity-settings/menu-tips` | 메뉴 팁 전체 조회 |
| `PUT` | `/entity-settings/menu-tips/:menuCode` | 메뉴 팁 저장/수정 |
| `GET` | `/entity-settings/site-config/login-modal` | 로그인 모달 데이터 (인증 사용자용) |
| `GET` | `/entity-settings/menu-tips/:menuCode` | 특정 메뉴 팁 조회 (사용자용) |

**사이드 임팩트**: 없음 — 신규 테이블/API, 기존 코드 미변경

---

### Phase 2: Frontend — Entity Settings 카드 + 사이트 설정 페이지

**목표**: Site Configure 카드 + 설정 UI

#### 2-1. 파일

| 파일 | 유형 | 설명 |
|------|------|------|
| `EntitySiteConfigPage.tsx` | Page | 사이트 설정 페이지 (모달 설정 + 팁 관리) |
| `useSiteConfig.ts` | Hook | React Query 훅 (API 호출) |

#### 2-2. 변경 파일

| 파일 | 변경 내용 |
|------|----------|
| `EntitySettingsPage.tsx` | Site Configure 카드 추가 |
| `router/index.tsx` | `/entity-settings/site-config` 라우트 추가 |
| `entitySettings.json` (ko/en/vi) | 번역 키 추가 |

**사이드 임팩트**: EntitySettingsPage 카드 1개 추가 (기존 카드 영향 없음)

---

### Phase 3: Frontend — 로그인 모달 + 메뉴 팁 배너

**목표**: 사용자 경험 컴포넌트

#### 3-1. 파일

| 파일 | 유형 | 설명 |
|------|------|------|
| `LoginAnnouncementModal.tsx` | Component | 로그인 안내 모달 |
| `MenuTipBanner.tsx` | Component | 메뉴별 팁 배너 (접기/펼치기) |

#### 3-2. 변경 파일

| 파일 | 변경 내용 |
|------|----------|
| `MainLayout.tsx` | `<LoginAnnouncementModal />` 추가 |

**사이드 임팩트**: MainLayout에 모달 1개 추가 (조건부 렌더링, 기존 기능 영향 없음)

---

### Phase 4: i18n + 빌드 검증

**목표**: 3개 언어 번역 + 빌드 확인

---

## 3. 변경 파일 목록

### 신규 파일
| 경로 | 설명 |
|------|------|
| `apps/api/src/domain/entity-settings/entity/entity-site-config.entity.ts` | DB Entity |
| `apps/api/src/domain/entity-settings/entity/entity-menu-tip.entity.ts` | DB Entity |
| `apps/api/src/domain/entity-settings/service/entity-site-config.service.ts` | Service |
| `apps/api/src/domain/entity-settings/controller/entity-site-config.controller.ts` | Controller |
| `apps/web/src/domain/entity-settings/pages/EntitySiteConfigPage.tsx` | 설정 페이지 |
| `apps/web/src/domain/entity-settings/hooks/useSiteConfig.ts` | React Query 훅 |
| `apps/web/src/global/components/LoginAnnouncementModal.tsx` | 로그인 모달 |
| `apps/web/src/global/components/MenuTipBanner.tsx` | 메뉴 팁 배너 |
| `sql/migration_site_config.sql` | 마이그레이션 SQL |

### 수정 파일
| 경로 | 변경 내용 |
|------|----------|
| `apps/api/src/domain/entity-settings/entity-settings.module.ts` | Entity/Controller 등록 |
| `apps/web/src/domain/entity-settings/pages/EntitySettingsPage.tsx` | 카드 추가 |
| `apps/web/src/router/index.tsx` | 라우트 추가 |
| `apps/web/src/layouts/MainLayout.tsx` | 로그인 모달 추가 |
| `apps/web/src/locales/ko/entitySettings.json` | 한국어 번역 |
| `apps/web/src/locales/en/entitySettings.json` | 영어 번역 |
| `apps/web/src/locales/vi/entitySettings.json` | 베트남어 번역 |

---

## 4. 사이드 임팩트 분석

| 변경 | 영향 범위 | 위험도 | 대응 |
|------|----------|--------|------|
| 카드 추가 | EntitySettingsPage 레이아웃 | 낮음 | 기존 카드 배열 끝에 추가 |
| MainLayout 모달 | 전체 레이아웃 | 낮음 | 조건부 렌더링, API 응답 없으면 미표시 |
| DB 테이블 추가 | DB | 없음 | 신규 테이블이라 기존 데이터 영향 없음 |
| 메뉴 팁 배너 | 각 페이지 | 낮음 | 컴포넌트 import만 추가, 기존 컨텐츠 밀림 없음 |

---

## 5. DB 마이그레이션

스테이징/프로덕션 수동 SQL 필요:

```sql
-- migration_site_config.sql
CREATE TABLE IF NOT EXISTS amb_entity_site_configs (
  esc_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ent_id UUID NOT NULL UNIQUE,
  esc_login_modal_enabled BOOLEAN NOT NULL DEFAULT false,
  esc_login_modal_title VARCHAR(200),
  esc_login_modal_content TEXT,
  esc_created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  esc_updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS amb_entity_menu_tips (
  emt_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ent_id UUID NOT NULL,
  emt_menu_code VARCHAR(50) NOT NULL,
  emt_title VARCHAR(200),
  emt_content TEXT,
  emt_is_active BOOLEAN NOT NULL DEFAULT true,
  emt_sort_order INT NOT NULL DEFAULT 0,
  emt_created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  emt_updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(ent_id, emt_menu_code)
);

CREATE INDEX idx_entity_menu_tips_entity ON amb_entity_menu_tips(ent_id);
```
