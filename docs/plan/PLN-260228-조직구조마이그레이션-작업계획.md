# 조직 구조 마이그레이션 실행 계획

- **기반 문서**: `docs/analysis/REQ-조직구조마이그레이션분석-20260228.md`
- **이슈 총계**: CRITICAL 7건, HIGH 12건, MEDIUM 11건 = 30건
- **예상 일정**: 13~15일

---

## 1. 이슈 전체 요약

### 🔴 CRITICAL (7건) — SQL 실행 실패 / FK 깨짐

| ID | 내용 | 대상 |
|----|------|------|
| C-1 | `amb_menu_permissions` 컬럼명 불일치 (`mpr_*` → 실제 `mpm_*`) | 마이그레이션 SQL |
| C-2 | `amb_groups` 기존 타임스탬프 컬럼 중복 (`grp_created_at` 등) | 마이그레이션 SQL |
| C-3 | `amb_menu_group_permissions` 테이블 완전 누락 | 마이그레이션 SQL |
| C-4 | `amb_work_items.wit_group_id` FK 미처리 | 마이그레이션 SQL |
| C-5 | `amb_todos.tdo_group_id` FK + GROUP 가시성 미처리 | 마이그레이션 SQL |
| C-6 | `amb_meeting_notes.mtn_group_id` FK 미처리 | 마이그레이션 SQL |
| C-7 | `amb_notices.ntc_group_id` FK 미처리 | 마이그레이션 SQL |

### 🟠 HIGH (12건) — 런타임 오류

| ID | 내용 | 대상 |
|----|------|------|
| H-1 | `SHARE_TARGET_TYPE.DEPARTMENT` enum 미처리 | packages/types |
| H-2 | ACL 서비스 하드코딩 가시성 비교 | access-control.service.ts |
| H-3 | `WORK_ITEM_VISIBILITY` enum / GROUP 불일치 | packages/types |
| H-4 | `DEPARTMENT_CODE` 상수 (AI Agent 연동) | packages/types |
| H-5 | Response DTO 네이밍 패턴 급변 | packages/types |
| H-6 | `wis_target_type` 기존 데이터 마이그레이션 누락 | 마이그레이션 SQL |
| H-7 | `wit_visibility = 'GROUP'` 기존 데이터 누락 | 마이그레이션 SQL |
| H-8 | `amb_invitations` (inv_department + inv_group_id) 미처리 | entity + SQL |
| H-9 | `amb_users.usr_department` — 15곳+ 가시성 핵심 | entity + 서비스 |
| H-10 | `amb_assets.ast_department` + asset 서비스 ~10곳 | entity + 서비스 |
| H-11 | `calendar.service.ts` DEPARTMENT 하드코딩 | 서비스 |
| H-12 | 6개 Response DTO의 department/groupId 필드 | packages/types |

### 🟡 MEDIUM (11건)

| ID | 내용 | 대상 |
|----|------|------|
| M-1 | Rollback SQL 동일 문제 | 롤백 SQL |
| M-2 | `mtn_department` 처리 방침 미결정 | 정책 |
| M-3 | E5xxx 에러 코드 범위 미등록 | 에러 체계 |
| M-4 | `grp_name` UNIQUE 제약 결정 필요 | DB 스키마 |
| M-5 | `department.constant.ts` 12개 부서 상수 | 프론트엔드 |
| M-6 | i18n 번역 파일 (3언어 × 6 NS) | 프론트엔드 |
| M-7 | `amb_user_groups.grp_id` FK | 마이그레이션 SQL |
| M-8 | 라우팅 URL `/chat/:department` 경로 | 프론트엔드 |
| M-9 | `DEPT_LEVEL` 상수 의미 변경 | packages/types |
| M-10 | `menu-group-permission.service.ts` + DTO | 백엔드 |
| M-11 | NestJS 모듈 의존성 체인 (5개 모듈) | 백엔드 |

---

## 2. 정책 결정 필요 사항 (10건)

| # | 결정 사항 | 선택지 | 권장 |
|---|----------|--------|------|
| D-1 | GROUP 가시성 데이터 처리 | (A) CELL로 변환 / (B) SHARED로 변환 / (C) 삭제 | (A) CELL |
| D-2 | SHARE_TARGET_TYPE.TEAM 의미 변경 | (A) CELL로 변경 / (B) TEAM 유지 + CELL 추가 | (B) TEAM 유지 + CELL 추가 |
| D-3 | Response DTO 필드명 패턴 | (A) DB 컬럼 기반 (`untId`) / (B) 도메인 의미 기반 (`unitId`) | (B) 도메인 의미 |
| D-4 | `cel_name` UNIQUE 제약 | (A) 유지 / (B) Entity 범위로 변경 / (C) 제거 | (B) Entity 범위 |
| D-5 | `mtn_department` 컬럼 처리 | (A) `mtn_unit`으로 변경 / (B) 그대로 유지 | (A) 변경 |
| D-6 | DEPARTMENT_CODE (AI Agent) | (A) UNIT_CODE로 변경 / (B) 그대로 유지 | (B) 유지 — 개념 독립 |
| D-7 | `usr_department` 컬럼 처리 | (A) `usr_unit`으로 변경 / (B) 유지 | (B) 유지 — AI Agent 라우팅 독립 |
| D-8 | `/chat/:department` URL 경로 | (A) `/chat/:unit`으로 변경 + 리다이렉트 / (B) 유지 | (B) 유지 |
| D-9 | Todo/Notice/Meeting GROUP 가시성 | (A) CELL로 통합 변환 / (B) 도메인별 다르게 처리 | (A) CELL |
| D-10 | `ast_department` 컬럼 처리 | (A) `ast_unit`으로 변경 / (B) 유지 | (A) 변경 |

---

## 3. 마이그레이션 SQL 수정 계획

### Phase 2 추가

```sql
-- 2.2a: 기존 타임스탬프 컬럼 리네이밍 (ADD COLUMN 대신)
ALTER TABLE amb_cells RENAME COLUMN grp_created_at TO cel_created_at;
ALTER TABLE amb_cells RENAME COLUMN grp_updated_at TO cel_updated_at;
ALTER TABLE amb_cells RENAME COLUMN grp_deleted_at TO cel_deleted_at;

-- 2.8: menu_group_permissions 테이블 처리
ALTER TABLE amb_menu_group_permissions RENAME COLUMN grp_id TO cel_id;
ALTER TABLE amb_menu_group_permissions RENAME TO amb_menu_cell_permissions;
```

### Phase 4 수정

```sql
-- 4.3: 올바른 컬럼명 사용
UPDATE amb_menu_permissions SET mpm_menu_code = 'UNITS' WHERE mpm_menu_code = 'DEPARTMENTS';

-- 4.4: user_menu_permissions
UPDATE amb_user_menu_permissions SET ump_menu_code = 'UNITS' WHERE ump_menu_code = 'DEPARTMENTS';

-- 4.5: work_items FK
ALTER TABLE amb_work_items RENAME COLUMN wit_group_id TO wit_cell_id;

-- 4.6: work_item_shares target_type
UPDATE amb_work_item_shares SET wis_target_type = 'UNIT' WHERE wis_target_type = 'DEPARTMENT';

-- 4.7: GROUP 가시성 (정책 결정 후)
-- UPDATE amb_work_items SET wit_visibility = 'CELL' WHERE wit_visibility = 'GROUP';

-- 4.8: todos FK + GROUP 가시성
ALTER TABLE amb_todos RENAME COLUMN tdo_group_id TO tdo_cell_id;
UPDATE amb_todos SET tdo_visibility = 'CELL' WHERE tdo_visibility = 'GROUP';

-- 4.9: meeting_notes FK
ALTER TABLE amb_meeting_notes RENAME COLUMN mtn_group_id TO mtn_cell_id;

-- 4.10: notices FK
ALTER TABLE amb_notices RENAME COLUMN ntc_group_id TO ntc_cell_id;

-- 4.11: invitations FK
ALTER TABLE amb_invitations RENAME COLUMN inv_group_id TO inv_cell_id;

-- 4.12: user_groups FK
ALTER TABLE amb_user_groups RENAME COLUMN grp_id TO cel_id;

-- 4.13: calendar 가시성 CHECK 제약 (DEPARTMENT → UNIT)
```

### Phase 5 수정

```sql
-- 올바른 컬럼명과 데이터 타입
INSERT INTO amb_menu_permissions (mpm_menu_code, mpm_role, mpm_accessible)
VALUES ('TEAMS', 'ADMIN', TRUE), ('TEAMS', 'MANAGER', TRUE), ('TEAMS', 'USER', TRUE);
```

---

## 4. 하드코딩된 가시성 서비스 수정 목록 (10개)

| 서비스 | 수정 내용 |
|--------|----------|
| `access-control.service.ts` | `'DEPARTMENT'` → `'UNIT'`, `'GROUP'` → `'CELL'`, `witGroupId` → `witCellId` |
| `calendar.service.ts` | `case 'DEPARTMENT':` → `case 'UNIT':` |
| `meeting-note.service.ts` | `'DEPARTMENT'` → `'UNIT'`, `mtnDepartment` → `mtnUnit`, `mtnGroupId` → `mtnCellId` |
| `notice.service.ts` | `'DEPARTMENT'` → `'UNIT'`, `ntcDepartment` → `ntcUnit`, `ntcGroupId` → `ntcCellId` |
| `search.service.ts` | `'DEPARTMENT'` → `'UNIT'`, `'GROUP'` → `'CELL'` |
| `group-access.service.ts` | `case 'DEPARTMENT':` → `case 'UNIT':` |
| `asset.service.ts` | `usrDepartment` → 정책 결정 후 처리 (~10곳) |
| `asset-request.service.ts` | `usrDepartment` 비교 → 정책 결정 후 처리 |
| `hierarchy.service.ts` | `'DEPARTMENT_HEAD'` → `'UNIT_HEAD'`, `'TEAM_LEAD'` 유지/변경 결정 |
| `work-item-sync.listener.ts` | `witGroupId: event.groupId` → `witCellId: event.cellId` |

---

## 5. 영향받는 파일 전체 (24건)

### 백엔드 엔티티 (리네이밍 필요)
- `department.entity.ts` → Unit 엔티티
- `group.entity.ts` → Cell 엔티티
- `user-group.entity.ts` → User-Cell 엔티티
- `user-dept-role.entity.ts` → User-Unit-Role 엔티티
- `menu-group-permission.entity.ts` → Menu-Cell-Permission 엔티티
- `work-item.entity.ts` — `wit_group_id` → `wit_cell_id`
- `work-item-share.entity.ts` — `wis_target_type` 값 변경
- `todo.entity.ts` — `tdo_group_id` → `tdo_cell_id`
- `meeting-note.entity.ts` — `mtn_group_id` → `mtn_cell_id`
- `notice.entity.ts` — `ntc_group_id` → `ntc_cell_id`
- `invitation.entity.ts` — `inv_group_id` → `inv_cell_id`
- `calendar.entity.ts` — `cal_visibility` CHECK 제약
- `asset.entity.ts` — `ast_department` (정책 결정 후)

### 공유 타입
- `domain.types.ts` — SHARE_TARGET_TYPE, WORK_ITEM_VISIBILITY, DEPT_LEVEL, 6개 Response DTO

### 프론트엔드
- `department.constant.ts` — 12개 부서 상수
- `VisibilityBadge.tsx` — DEPARTMENT 배지
- `router/index.tsx` — `/chat/:department`, `settings/departments`
- `SubMenuLayout.tsx`, `ChatMenuGuard.tsx`, `chat.store.ts`, `ChatPage.tsx`
- i18n: 3언어 × 6 네임스페이스

### NestJS 모듈
- `acl.module.ts`, `members.module.ts`, `invitation.module.ts`, `calendar.module.ts`, `agent.module.ts`

---

## 6. 실행 순서 (권장)

### Step 1: 정책 결정 (D-1 ~ D-10)
→ 이 결정이 이후 모든 작업의 방향을 결정

### Step 2: 마이그레이션 SQL 수정
→ CRITICAL 7건 반영, Phase 2/4/5 수정

### Step 3: 롤백 SQL 수정
→ 마이그레이션 SQL과 대칭으로 수정

### Step 4: 백엔드 엔티티 + 서비스 수정
→ 13개 엔티티, 10개 서비스, DTO, 모듈 의존성

### Step 5: 공유 타입 수정
→ domain.types.ts의 enum/interface/Response DTO

### Step 6: 프론트엔드 수정
→ 상수, 컴포넌트, 라우팅, 스토어, i18n

### Step 7: 로컬 DB 테스트
→ 마이그레이션 실행 → 앱 기동 → 롤백 테스트

### Step 8: 스테이징 배포 및 검증
