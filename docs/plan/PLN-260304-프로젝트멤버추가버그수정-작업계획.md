# 작업 계획서: 프로젝트 멤버 추가 기능 버그 수정

**문서번호**: PLAN-프로젝트멤버추가버그수정-작업계획-20260304
**작성일**: 2026-03-04
**관련 분석서**: REQ-프로젝트멤버추가기능버그수정-20260304

---

## 1. 시스템 개발 현황 분석

### 1.1 영향받는 파일
| 파일 | 역할 | 변경 유형 |
|------|------|----------|
| `apps/api/src/domain/project/mapper/project.mapper.ts` | 엔티티→응답 변환 | Bug Fix |
| `apps/web/src/domain/project/components/ProjectMemberList.tsx` | 멤버 관리 UI | 기능 개선 |

### 1.2 토스트 시스템
- **라이브러리**: `sonner` (Toaster)
- **사용 패턴**: `import { toast } from 'sonner'` → `toast.success()` / `toast.error()`
- **i18n**: 번역 키 기반 메시지

---

## 2. 구현 계획

### Step 1: Mapper 속성명 수정 (Backend)
**파일**: `apps/api/src/domain/project/mapper/project.mapper.ts`

**변경 내용**:
```typescript
// toMemberResponse (line 54-66)
userName: entity.user?.name || ''      → entity.user?.usrName || ''
userEmail: entity.user?.email || ''    → entity.user?.usrEmail || ''

// toResponse (line 22-24)
proposerName: entity.proposer?.name || ''    → entity.proposer?.usrName || ''
managerName: entity.manager?.name || null    → entity.manager?.usrName || null
```

### Step 2: 프론트엔드 토스트 피드백 추가 (Frontend)
**파일**: `apps/web/src/domain/project/components/ProjectMemberList.tsx`

**변경 내용**:
- `import { toast } from 'sonner'` 추가
- `handleAdd()`: onSuccess에 `toast.success()` 추가
- `handleAdd()`: onError 콜백 추가 + `toast.error()` 표시
- `handleRemove()`: onSuccess/onError 콜백 추가

### Step 3: i18n 번역 키 추가
**파일**: 해당 locale 파일 (project 네임스페이스)

---

## 3. 사이드 임팩트 분석

| 영향 범위 | 상세 | 위험도 |
|----------|------|--------|
| 프로젝트 목록 페이지 | proposerName/managerName 정상 표시 | 긍정적 |
| 프로젝트 상세 페이지 | 멤버/관리자/제안자 이름 정상 표시 | 긍정적 |
| 프로젝트 멤버 목록 | 기존 멤버 이름/이메일도 정상 표시 | 긍정적 |
| 다른 모듈 | 영향 없음 (Project Mapper만 수정) | 없음 |

---

## 4. 작업 순서

1. Backend Mapper 수정 → 빌드 확인
2. Frontend 토스트 피드백 추가 → 빌드 확인
3. 커밋 & 스테이징 배포
