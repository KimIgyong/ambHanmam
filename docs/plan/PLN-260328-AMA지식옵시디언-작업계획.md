# 작업 계획서: AMA 지식 옵시디언

> **문서번호**: PLAN-AMA지식옵시디언-작업계획-20260328  
> **작성일**: 2026-03-28 (Rev.1: 2026-03-29)  
> **참조**: REQ-AMA지식옵시디언-20260328  
> **상태**: Draft

---

## 1. 시스템 개발 현황 분석

### 대상 디렉토리 구조

```
apps/api/src/domain/
├── today/                    # 오늘의 미션 (mission.service.ts)
├── todo/                     # Task/ToDo (todo.service.ts)
├── issues/                   # 이슈 (issues.service.ts)
├── project/                  # 프로젝트 (project.service.ts)
├── meeting-notes/            # 노트 (meeting-notes.service.ts) ← 확장 대상
│   ├── entity/
│   │   ├── meeting-note.entity.ts         # mtn_folder_id FK 포함
│   │   ├── meeting-note-folder.entity.ts  # ✅ 구현 완료
│   │   ├── meeting-note-participant.entity.ts
│   │   ├── meeting-note-comment.entity.ts
│   │   ├── meeting-note-rating.entity.ts
│   │   ├── meeting-note-project.entity.ts # 노트↔프로젝트 M:N
│   │   └── meeting-note-issue.entity.ts   # 노트↔이슈 M:N
│   ├── service/
│   │   └── meeting-note.service.ts        # ✅ 폴더 CRUD 포함 (getFolders, createFolder, updateFolder, deleteFolder)
│   └── controller/
│       └── meeting-note.controller.ts     # ✅ 폴더 API (/folders)
└── kms/                      # KMS 태그 시스템

apps/web/src/domain/
├── today/                    # TodayPage, MissionSection
├── todos/                    # TodoPage
├── issues/                   # IssuePage
├── project/                  # ProjectPage
├── meeting-notes/            # MeetingNotesPage, RichTextEditor ← 확장 대상
│   ├── components/
│   │   ├── RichTextEditor.tsx          # TipTap v3.20
│   │   ├── NoteFolderSidebar.tsx       # ✅ 구현 완료 (폴더 목록/생성/수정/삭제 UI)
│   │   ├── NoteCommentSection.tsx
│   │   └── MeetingNoteFormModal.tsx    # ✅ 폴더 선택 포함
│   ├── hooks/
│   │   └── useMeetingNotes.ts          # ✅ useMeetingNoteFolders, useCreateFolder, useUpdateFolder, useDeleteFolder
│   ├── service/
│   │   └── meeting-note.service.ts     # ✅ getFolders, createFolder, updateFolder, deleteFolder
│   └── pages/
│       ├── MeetingNotesPage.tsx         # ✅ NoteFolderSidebar 통합
│       └── MeetingNoteDetailPage.tsx
└── kms/                      # TagCloud, KnowledgeGraph
```

### 이미 구현된 기능 (AS-IS)

| 기능 | 백엔드 | 프론트엔드 | DB |
|------|--------|-----------|-----|
| **노트 폴더** | Entity + Service CRUD + Controller API | NoteFolderSidebar + hooks + service | `amb_meeting_note_folders` 테이블 + `mtn_folder_id` FK |
| **노트↔프로젝트 연결** | `MeetingNoteProjectEntity` | 생성 시 선택 | `amb_meeting_note_projects` 테이블 |
| **노트↔이슈 연결** | `MeetingNoteIssueEntity` | 생성 시 선택 | `amb_meeting_note_issues` 테이블 |
| **노트 검색 (기본)** | `search` 쿼리 파라미터 (제목/내용 LIKE) | 검색 입력 필드 | - |

> ⚠️ **참고**: 현재 폴더 구조는 **플랫 구조** (parent_id 없음). 계층 폴더는 미지원.

### 기술 스택

| 영역 | 현재 | 추가 필요 |
|------|------|----------|
| Editor | TipTap v3.20 (HTML) | WikiLink Extension (커스텀 개발) |
| Graph | 없음 | `react-force-graph-2d` |
| Search | LIKE 기반 (제목/내용) | PostgreSQL FTS (`tsvector` + GIN) |
| Folder | 플랫 폴더 (완료) | - (현재 수준 유지) |
| DB | TypeORM + PostgreSQL 15 | 2개 신규 테이블 + 1개 ALTER |

### 제약사항

1. 스테이징/프로덕션 DB synchronize OFF → 모든 스키마 변경 수동 SQL 필수
2. react-force-graph-2d는 동적 import 필수 (번들 크기 관리)
3. `ent_id` 기반 데이터 격리 모든 테이블에 적용

---

## 2. 단계별 구현 계획

### Phase 1: 데이터 기반 — DB 스키마 + Entity (백엔드)

> **목표**: 노트 링크, 노트↔태스크 연결 테이블 생성 + FTS 컬럼 추가

#### STEP 1-1: 노트 링크 테이블 생성

- **파일**: `apps/api/src/domain/meeting-notes/entity/note-link.entity.ts` (신규)
- **변경 내용**:
```typescript
@Entity('amb_note_links')
export class NoteLinkEntity {
  @PrimaryGeneratedColumn('uuid', { name: 'nlk_id' })
  nlkId: string;

  @Column({ name: 'ent_id', type: 'uuid' })
  entId: string;

  @Column({ name: 'nlk_source_note_id', type: 'uuid' })
  nlkSourceNoteId: string;  // FK → amb_meeting_notes

  @Column({ name: 'nlk_target_note_id', type: 'uuid', nullable: true })
  nlkTargetNoteId: string | null;  // FK → amb_meeting_notes (null = 미존재 노트)

  @Column({ name: 'nlk_link_text', type: 'varchar', length: 500 })
  nlkLinkText: string;

  @Column({ name: 'nlk_target_type', type: 'varchar', length: 20, default: 'NOTE' })
  nlkTargetType: string;  // NOTE | MISSION | TASK | ISSUE | PROJECT

  @Column({ name: 'nlk_target_ref_id', type: 'uuid', nullable: true })
  nlkTargetRefId: string | null;  // 타입별 참조 ID (non-NOTE)

  @Column({ name: 'nlk_context', type: 'text', nullable: true })
  nlkContext: string | null;  // 주변 텍스트 스니펫

  @CreateDateColumn({ name: 'nlk_created_at' })
  nlkCreatedAt: Date;
}
```
- **사이드 임팩트**: 없음 (신규 테이블)

#### STEP 1-2: 노트↔태스크 연결 테이블

- **파일**: `apps/api/src/domain/meeting-notes/entity/meeting-note-todo.entity.ts` (신규)
- **변경 내용**:
```typescript
@Entity('amb_meeting_note_todos')
export class MeetingNoteTodoEntity {
  @PrimaryGeneratedColumn('uuid', { name: 'mnt_id' })
  mntId: string;

  @Column({ name: 'mtn_id', type: 'uuid' })
  mtnId: string;  // FK → amb_meeting_notes

  @Column({ name: 'tdo_id', type: 'uuid' })
  tdoId: string;  // FK → amb_todos

  @CreateDateColumn({ name: 'mnt_created_at' })
  mntCreatedAt: Date;
}
```
- **사이드 임팩트**: 없음 (신규 테이블)

#### STEP 1-3: 기존 테이블 ALTER (FTS 컬럼)

- **파일**: `meeting-note.entity.ts` (수정)
- **변경 내용**: `mtnSearchVector` (tsvector) 컬럼 추가 (select: false)
- **사이드 임팩트**: 기존 쿼리 영향 없음 (select: false)

#### STEP 1-4: 마이그레이션 SQL

```sql
-- ============================================================
-- Migration: AMA Knowledge Obsidian - Phase 1
-- ============================================================

-- 1. 노트 링크 테이블
CREATE TABLE IF NOT EXISTS amb_note_links (
  nlk_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ent_id UUID NOT NULL,
  nlk_source_note_id UUID NOT NULL REFERENCES amb_meeting_notes(mtn_id) ON DELETE CASCADE,
  nlk_target_note_id UUID REFERENCES amb_meeting_notes(mtn_id) ON DELETE SET NULL,
  nlk_link_text VARCHAR(500) NOT NULL,
  nlk_target_type VARCHAR(20) NOT NULL DEFAULT 'NOTE',
  nlk_target_ref_id UUID,
  nlk_context TEXT,
  nlk_created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_note_links_source ON amb_note_links(nlk_source_note_id);
CREATE INDEX IF NOT EXISTS idx_note_links_target ON amb_note_links(nlk_target_note_id);
CREATE INDEX IF NOT EXISTS idx_note_links_ent ON amb_note_links(ent_id);
CREATE UNIQUE INDEX IF NOT EXISTS idx_note_links_unique
  ON amb_note_links(nlk_source_note_id, nlk_link_text, nlk_target_type);

-- 2. 노트↔태스크 연결 테이블
CREATE TABLE IF NOT EXISTS amb_meeting_note_todos (
  mnt_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  mtn_id UUID NOT NULL REFERENCES amb_meeting_notes(mtn_id) ON DELETE CASCADE,
  tdo_id UUID NOT NULL REFERENCES amb_todos(tdo_id) ON DELETE CASCADE,
  mnt_created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_mnt_unique ON amb_meeting_note_todos(mtn_id, tdo_id);

-- 3. FTS 컬럼 추가 (기존 테이블)
ALTER TABLE amb_meeting_notes ADD COLUMN IF NOT EXISTS mtn_search_vector tsvector;
CREATE INDEX IF NOT EXISTS idx_meeting_notes_fts
  ON amb_meeting_notes USING GIN(mtn_search_vector);
```

#### STEP 1-5: 모듈 등록

- **파일**: `meeting-notes.module.ts` (수정)
- **변경 내용**: `NoteLinkEntity`, `MeetingNoteTodoEntity`를 `TypeOrmModule.forFeature()`에 추가

---

### Phase 2: 양방향 링크 핵심 — WikiLink Extension + 백링크 서비스

> **목표**: `[[Wiki Link]]` 입력/저장/조회 + 백링크 패널

#### STEP 2-1: 노트 링크 서비스 (백엔드)

- **파일**: `apps/api/src/domain/meeting-notes/service/note-link.service.ts` (신규)
- **변경 내용**:
  - `parseAndSaveLinks(noteId, htmlContent, entityId)` — HTML에서 WikiLink 노드 파싱 → amb_note_links UPSERT (기존 링크 비교, 삭제된 링크 제거)
  - `getBacklinks(noteId, entityId)` — 해당 노트를 참조하는 노트 목록 반환
  - `resolveLink(linkText, targetType, entityId)` — 링크 텍스트 → 실제 대상 ID resolve
  - `searchNotesForAutocomplete(query, entityId, userId)` — `[[` 입력 시 자동완성용
- **사이드 임팩트**: meeting-notes.service.ts의 `save/update` 메서드에 `parseAndSaveLinks` 호출 추가

#### STEP 2-2: 노트 링크 컨트롤러 (백엔드)

- **파일**: `meeting-notes.controller.ts` (수정, 엔드포인트 추가)
- **변경 내용**:
  - `GET /meeting-notes/:id/backlinks` — 백링크 목록
  - `GET /meeting-notes/link-autocomplete?q=&type=` — WikiLink 자동완성 (노트/미션/태스크/이슈/프로젝트 통합 검색)
  - `POST /meeting-notes/:id/resolve-link` — 링크 resolve (미존재 노트 시 자동 생성 옵션)

#### STEP 2-3: TipTap WikiLink Extension (프론트엔드)

- **파일**: `apps/web/src/domain/meeting-notes/components/extensions/WikiLinkExtension.ts` (신규)
- **변경 내용**:
  - TipTap Node Extension 구현: `name: 'wikiLink'`
  - `[[` 키 입력 트리거 → suggestion plugin (TipTap Suggestion 패턴)
  - 자동완성 드롭다운 UI (API 호출하여 노트/미션/태스크/이슈/프로젝트 검색)
  - 렌더링: `<span class="wiki-link" data-type="NOTE" data-id="uuid">제목</span>`
  - HTML 시리얼라이즈/파싱 규칙 정의
  - 타입 프리픽스 지원: `[[미션:`, `[[태스크:`, `[[이슈:#`, `[[프로젝트:`

#### STEP 2-4: WikiLink Extension 등록

- **파일**: `RichTextEditor.tsx` (수정)
- **변경 내용**: WikiLinkExtension을 에디터 extensions 배열에 추가

#### STEP 2-5: 백링크 패널 (프론트엔드)

- **파일**: `apps/web/src/domain/meeting-notes/components/BacklinkPanel.tsx` (신규)
- **변경 내용**:
  - 노트 상세 페이지 하단에 "이 노트를 참조하는 문서" 패널
  - 참조 타입별 아이콘 (노트/미션/태스크/이슈/프로젝트)
  - 클릭 시 해당 소스 노트로 이동
- **파일**: `MeetingNoteDetailPage.tsx` (수정) — BacklinkPanel 삽입

---

### Phase 3: Full-text 검색 강화

> **목표**: PostgreSQL tsvector 기반 고급 검색 + 커맨드 팔레트 UI

#### STEP 3-1: Full-text 검색 서비스 (백엔드)

- **파일**: `meeting-notes.service.ts` (수정)
- **변경 내용**:
  - `fullTextSearch(query, entityId, userId, scope)` 메서드 추가
  - 노트 저장 시 tsvector 업데이트: `mtn_search_vector = to_tsvector('simple', mtn_title || ' ' || strip_tags(mtn_content))`
  - `ts_headline`으로 매칭 스니펫 반환
  - 검색 API: `GET /meeting-notes/search?q=&scope=MY|ENTITY`

#### STEP 3-2: 검색 UI — 커맨드 팔레트 (프론트엔드)

- **파일**: `apps/web/src/domain/meeting-notes/components/NoteSearch.tsx` (신규)
- **변경 내용**:
  - `Ctrl+K` / `Cmd+K` 커맨드 팔레트 형태
  - 실시간 검색 (debounce 300ms)
  - 결과: 제목 + 스니펫 + 날짜 + 폴더명, 클릭 시 노트 이동
- **파일**: `MeetingNotesPage.tsx` (수정) — 검색 아이콘 버튼 추가

---

### Phase 4: 그래프 뷰

> **목표**: 노트 및 업무 항목 간 관계 시각화

#### STEP 4-1: 그래프 데이터 API (백엔드)

- **파일**: `meeting-notes.controller.ts` (수정, 엔드포인트 추가)
- **변경 내용**:
  - `GET /meeting-notes/graph?scope=MY|ENTITY&center_id=` — 노드/엣지 데이터 반환
  - 노드: `{ id, type, label, color, size }`
  - 엣지: `{ source, target, type }` (WikiLink, 프로젝트 연결, 이슈 연결, 태스크 연결)
  - scope 파라미터로 MY(내 노트) / ENTITY(전체) 스코프
  - center_id: 로컬 그래프 중심 노트 (옵션, 2-depth 서브그래프)
  - 최대 500노드 제한

#### STEP 4-2: 그래프 뷰 페이지 (프론트엔드)

- **파일**: `apps/web/src/domain/meeting-notes/pages/NoteGraphPage.tsx` (신규)
- **변경 내용**:
  - `react-force-graph-2d` 동적 import (`React.lazy`)
  - 노드 색상: 노트(#6366f1), 태스크(#8b5cf6), 이슈(#ef4444), 프로젝트(#22c55e), 미션(#f59e0b)
  - 노드 크기: 연결 수 비례 (min 4, max 16)
  - 노드 클릭: 해당 항목 상세 페이지로 이동 or 로컬 그래프 전환
  - 필터 패널: 타입 토글, 날짜 범위
  - 전체/로컬 그래프 전환 버튼
- **사이드 임팩트**: 라우터에 `/meeting-notes/graph` 경로 추가

#### STEP 4-3: 패키지 설치

- **변경**: `apps/web/package.json` — `react-force-graph-2d` 추가
- **빌드**: 동적 import로 코드 스플릿 → 메인 번들 영향 최소화

---

### Phase 5: 데일리 노트 + 미션 연동

> **목표**: 미션 작성 시 데일리 노트 자동 생성/연동

#### STEP 5-1: 데일리 노트 서비스 (백엔드)

- **파일**: `apps/api/src/domain/meeting-notes/service/daily-note.service.ts` (신규)
- **변경 내용**:
  - `getOrCreateDailyNote(userId, entityId, date)` — 해당 날짜 데일리 노트 조회 or 생성 (기존 `amb_meeting_note_folders`의 "데일리 노트" 폴더에 자동 배정)
  - `appendMissionContent(noteId, missionContent)` — 미션 내용 데일리 노트에 추가
  - `appendCheckResult(noteId, checkResult)` — 미션 체크 결과 요약 추가
  - `appendTaskLinks(noteId, taskIds)` — 생성된 태스크 WikiLink 자동 삽입

#### STEP 5-2: 미션 서비스 연동 (백엔드)

- **파일**: `apps/api/src/domain/today/service/mission.service.ts` (수정)
- **변경 내용**:
  - `saveMission()` 완료 후 → `dailyNoteService.getOrCreateDailyNote()` + `appendMissionContent()` 호출
  - `saveCheck()` 완료 후 → `dailyNoteService.appendCheckResult()` + `appendTaskLinks()` 호출
- **사이드 임팩트**: MissionModule에 MeetingNotesModule import 추가 (순환 의존성 주의 → forwardRef)

#### STEP 5-3: 데일리 노트 UI (프론트엔드)

- **파일**: `MeetingNotesPage.tsx` (수정) 또는 `TodayPage.tsx` (수정)
- **변경 내용**:
  - "오늘의 노트" 바로가기 버튼 (TodayPage 상단 or 사이드바)
  - 데일리 노트 자동 포커스 (MeetingNotesPage에서 오늘 날짜 노트 하이라이트)

---

### Phase 6: i18n + 라우터 + 메뉴

> **목표**: 번역, 라우팅, 메뉴 등록

#### STEP 6-1: i18n 번역

- **파일**: `apps/web/src/locales/{ko,en,vi}/meetingNotes.json` (수정)
- **변경 내용**: 그래프, WikiLink, 백링크, 검색 관련 번역 키 추가

#### STEP 6-2: 라우터 등록

- **파일**: `apps/web/src/router/` (수정)
- **변경 내용**: `/meeting-notes/graph` 경로 추가

---

## 3. 변경 파일 목록

### 신규 파일 (Backend)

| # | 파일 | 내용 |
|---|------|------|
| 1 | `apps/api/.../meeting-notes/entity/note-link.entity.ts` | 노트 링크 엔티티 |
| 2 | `apps/api/.../meeting-notes/entity/meeting-note-todo.entity.ts` | 노트↔태스크 연결 엔티티 |
| 3 | `apps/api/.../meeting-notes/service/note-link.service.ts` | 링크 파싱/저장/백링크 조회 |
| 4 | `apps/api/.../meeting-notes/service/daily-note.service.ts` | 데일리 노트 자동 생성/연동 |

### 신규 파일 (Frontend)

| # | 파일 | 내용 |
|---|------|------|
| 5 | `apps/web/.../meeting-notes/components/extensions/WikiLinkExtension.ts` | TipTap WikiLink Extension |
| 6 | `apps/web/.../meeting-notes/components/BacklinkPanel.tsx` | 백링크 패널 |
| 7 | `apps/web/.../meeting-notes/components/NoteSearch.tsx` | 전역 검색 (Cmd+K) |
| 8 | `apps/web/.../meeting-notes/pages/NoteGraphPage.tsx` | 그래프 뷰 페이지 |

### 수정 파일

| # | 파일 | 변경 내용 |
|---|------|----------|
| 9 | `meeting-note.entity.ts` | mtn_search_vector 컬럼 추가 |
| 10 | `meeting-notes.module.ts` | NoteLinkEntity, MeetingNoteTodoEntity 등록 + NoteLinkService, DailyNoteService 프로바이더 등록 |
| 11 | `meeting-notes.service.ts` | 링크 파싱 호출 (save/update 시), fullTextSearch 메서드 추가 |
| 12 | `meeting-notes.controller.ts` | 백링크/검색/그래프/자동완성 API 엔드포인트 추가 |
| 13 | `RichTextEditor.tsx` | WikiLink Extension 등록 |
| 14 | `MeetingNoteDetailPage.tsx` | 백링크 패널 삽입 |
| 15 | `MeetingNotesPage.tsx` | 검색 버튼 추가 |
| 16 | `mission.service.ts` | 데일리 노트 연동 호출 |
| 17 | `locales/{ko,en,vi}/meetingNotes.json` | WikiLink/백링크/그래프/검색 번역 키 추가 |
| 18 | 라우터 파일 | `/meeting-notes/graph` 경로 |
| 19 | `apps/web/package.json` | react-force-graph-2d 추가 |

### 기존 구현 (변경 불필요)

| 기능 | 파일 | 비고 |
|------|------|------|
| 노트 폴더 엔티티 | `meeting-note-folder.entity.ts` | `amb_meeting_note_folders` 테이블 |
| 노트 폴더 CRUD 서비스 | `meeting-note.service.ts` (getFolders, createFolder, updateFolder, deleteFolder) | 기 구현 |
| 노트 폴더 API | `meeting-note.controller.ts` (GET/POST/PATCH/DELETE /folders) | 기 구현 |
| 노트 폴더 사이드바 UI | `NoteFolderSidebar.tsx` | 기 구현 |
| 노트 폴더 hooks | `useMeetingNotes.ts` (useMeetingNoteFolders, useCreateFolder, useUpdateFolder, useDeleteFolder) | 기 구현 |
| 노트 폴더 서비스 | `meeting-note.service.ts` (getFolders, createFolder, updateFolder, deleteFolder) | 기 구현 |
| 공유 타입 | `packages/types` - MeetingNoteFolderResponse | 기 구현 |
| DB 마이그레이션 | `sql/migration_meeting_note_folders.sql` | 기 적용 |

---

## 4. 사이드 임팩트 분석

| 범위 | 위험도 | 설명 |
|------|--------|------|
| Meeting Notes 저장 | 🟡 중간 | 노트 save/update에 링크 파싱 로직 추가 — 저장 성능 약간 증가 (HTML 파싱 O(n)) |
| Mission 서비스 | 🟡 중간 | saveMission/saveCheck에 데일리 노트 연동 — 순환 의존성 주의 (forwardRef) |
| RichTextEditor | 🟢 낮음 | Extension 추가만, 기존 기능 영향 없음 |
| 번들 크기 | 🟢 낮음 | react-force-graph-2d는 동적 import로 코드 스플릿 |
| DB 성능 | 🟢 낮음 | 신규 테이블, 적절한 인덱스 설계 완료 |
| 기존 폴더 기능 | 🟢 없음 | 기존 `NoteFolderSidebar`, 폴더 CRUD 변경 없음 — 그대로 유지 |
| 데이터 격리 | 🟢 낮음 | 모든 신규 테이블에 ent_id 포함, OwnEntityGuard 패턴 준수 |

---

## 5. DB 마이그레이션 전략

### 스테이징 마이그레이션 SQL

Phase 1 구현 후 스테이징 배포 전 실행:

```bash
ssh amb-staging "docker exec amb-postgres-staging psql -U amb_user -d db_amb" <<'SQL'
-- Phase 1: 2개 신규 테이블 + FTS 컬럼 ALTER
-- (STEP 1-4의 SQL 그대로 실행)
SQL
```

### 롤백 전략

```sql
-- 롤백 시
DROP TABLE IF EXISTS amb_meeting_note_todos;
DROP TABLE IF EXISTS amb_note_links;
ALTER TABLE amb_meeting_notes DROP COLUMN IF EXISTS mtn_search_vector;
DROP INDEX IF EXISTS idx_meeting_notes_fts;
```

---

## 6. 구현 순서 및 검증

| Phase | 내용 | 빌드 검증 | 배포 전 DB |
|-------|------|----------|-----------|
| **1** | DB Entity + 모듈 등록 | `npx tsc --noEmit` (api) | SQL 마이그레이션 |
| **2** | WikiLink Extension + 백링크 | `npm run -w @amb/web build` | 없음 |
| **3** | FTS 검색 강화 | `npm run -w @amb/web build` | 없음 |
| **4** | 그래프 뷰 | `npm run -w @amb/web build` | 없음 |
| **5** | 데일리 노트 + 미션 연동 | 양쪽 빌드 | 없음 |
| **6** | i18n + 라우터 + 메뉴 | `npm run -w @amb/web build` | 없음 |

Phase 1 완료 → 스테이징 배포 → Phase 2-6 → 최종 스테이징 배포

---

## 변경 이력

| 버전 | 일자 | 변경 내용 |
|------|------|----------|
| Rev.0 | 2026-03-28 | 초안 작성 |
| Rev.1 | 2026-03-29 | 노트 폴더 기능 기 구현 반영 — Phase 1에서 폴더 테이블 제거, Phase 3에서 폴더 트리/서비스 제거, 변경 파일 목록 정리 |
