# TC-담당자알림팝업 테스트 케이스

- **작성일**: 2026-03-01
- **관련 문서**: REQ-담당자알림팝업-20260301.md, PLAN-담당자알림팝업-작업계획-20260301.md
- **PR**: #12

---

## 1. 단위 테스트 케이스

### T-1. 할일(Todo) 담당자 지정 시 알림 생성

| 항목 | 내용 |
|------|------|
| **전제 조건** | 사용자 A(admin)가 로그인, 사용자 B(test)가 존재 |
| **테스트 절차** | 1. POST /api/v1/todos로 할일 생성, participant_ids에 사용자 B의 ID 포함 |
| **기대 결과** | amb_notifications 테이블에 ntf_type=TODO_ASSIGNED, ntf_recipient_id=사용자B ID, ntf_sender_id=사용자A ID 레코드 생성 |
| **실제 결과** | ✅ PASS - DB에 정확히 1건 생성됨. ntf_title="amoeba님이 할일의 참여자로 지정했습니다." |

### T-2. 본인 제외 규칙

| 항목 | 내용 |
|------|------|
| **전제 조건** | 사용자 A가 로그인 |
| **테스트 절차** | 1. POST /api/v1/todos로 할일 생성, participant_ids에 사용자 A 본인의 ID 포함 |
| **기대 결과** | 사용자 A에 대한 알림은 생성되지 않음 (본인에게는 알림 불필요) |
| **실제 결과** | ✅ PASS - 본인 ID가 assigneeIds에 포함되어도 senderId와 같으므로 알림 미생성 |

### T-3. 이슈(Issue) 담당자 지정 시 알림 생성

| 항목 | 내용 |
|------|------|
| **전제 조건** | 사용자 A가 로그인, 사용자 B가 존재 |
| **테스트 절차** | 1. POST /api/v1/issues로 이슈 생성, assignee_ids에 사용자 B의 ID 포함 |
| **기대 결과** | amb_notifications에 ntf_type=ISSUE_ASSIGNED 레코드 생성 |
| **실제 결과** | ✅ PASS (코드 분기 확인) |

### T-4. 회의록(MeetingNote) 참여자 지정 시 알림 생성

| 항목 | 내용 |
|------|------|
| **전제 조건** | 사용자 A가 로그인, 사용자 B가 존재 |
| **테스트 절차** | 1. POST /api/v1/meeting-notes로 회의록 생성, participant_ids에 사용자 B의 ID 포함 |
| **기대 결과** | amb_notifications에 ntf_type=NOTE_ASSIGNED 레코드 생성 |
| **실제 결과** | ✅ PASS (코드 분기 확인) |

---

## 2. API 엔드포인트 테스트

### T-5. 알림 목록 조회 (GET /notifications)

| 항목 | 내용 |
|------|------|
| **전제 조건** | 인증된 사용자, X-Entity-Id 헤더 포함 |
| **테스트 절차** | 1. GET /api/v1/notifications |
| **기대 결과** | `{ success: true, data: { data: [...], total: N } }` 형태 응답 |
| **실제 결과** | ✅ PASS - 200 OK, `{"success":true,"data":{"data":[],"total":0}}` |

### T-6. 미읽음 카운트 조회 (GET /notifications/unread-count)

| 항목 | 내용 |
|------|------|
| **전제 조건** | 인증된 사용자 |
| **테스트 절차** | 1. GET /api/v1/notifications/unread-count |
| **기대 결과** | `{ success: true, data: { count: N } }` 형태 응답 |
| **실제 결과** | ✅ PASS - 200 OK, `{"success":true,"data":{"count":0}}` |

### T-7. 전체 읽음 처리 (PATCH /notifications/read-all)

| 항목 | 내용 |
|------|------|
| **전제 조건** | 인증된 사용자, 미읽음 알림 존재 |
| **테스트 절차** | 1. PATCH /api/v1/notifications/read-all |
| **기대 결과** | `{ success: true }` 응답, 해당 사용자의 모든 알림 ntf_is_read=true 변경 |
| **실제 결과** | ✅ PASS - 200 OK, `{"success":true}` |

### T-8. 단일 읽음 처리 (PATCH /notifications/:id/read)

| 항목 | 내용 |
|------|------|
| **전제 조건** | 인증된 사용자, 특정 알림 ID 보유 |
| **테스트 절차** | 1. PATCH /api/v1/notifications/{ntf_id}/read |
| **기대 결과** | `{ success: true }` 응답, 해당 알림만 ntf_is_read=true로 변경 |
| **실제 결과** | ✅ PASS (코드 로직 확인) |

---

## 3. SSE 연결 테스트

### T-9. SSE 스트림 연결

| 항목 | 내용 |
|------|------|
| **전제 조건** | 인증된 사용자, 브라우저에서 접속 |
| **테스트 절차** | 1. GET /api/v1/notifications/stream (EventSource) |
| **기대 결과** | SSE 연결 수립, 주기적으로 heartbeat 이벤트 수신 |
| **실제 결과** | - (프론트엔드 통합 테스트 시 검증 예정) |

### T-10. SSE 실시간 알림 수신

| 항목 | 내용 |
|------|------|
| **전제 조건** | 사용자 B가 SSE 연결 중, 사용자 A가 할일 생성 |
| **테스트 절차** | 1. 사용자 B 로그인 + SSE 연결 2. 사용자 A가 할일 생성 (participant_ids에 B 포함) |
| **기대 결과** | 사용자 B에게 실시간으로 notification 이벤트 발송, 토스트 팝업 표시 |
| **실제 결과** | - (멀티 사용자 환경에서 통합 테스트 시 검증 예정) |

---

## 4. 프론트엔드 UI 테스트

### T-11. NotificationBell 렌더링

| 항목 | 내용 |
|------|------|
| **전제 조건** | 로그인 후 대시보드 접속 |
| **테스트 절차** | 1. 메인 레이아웃 헤더에서 알림 벨 아이콘 확인 |
| **기대 결과** | Bell 아이콘 표시, 미읽음 알림 존재 시 빨간 배지(숫자) 표시 |
| **실제 결과** | - (프론트엔드 통합 테스트 시 검증 예정) |

### T-12. 알림 드롭다운 목록

| 항목 | 내용 |
|------|------|
| **전제 조건** | 알림이 1건 이상 존재 |
| **테스트 절차** | 1. 벨 아이콘 클릭 2. 드롭다운 목록 확인 |
| **기대 결과** | 알림 목록 표시 (제목, 메시지, 시간), 미읽음은 구분 스타일 |
| **실제 결과** | - (프론트엔드 통합 테스트 시 검증 예정) |

### T-13. 알림 클릭 시 해당 리소스로 이동

| 항목 | 내용 |
|------|------|
| **전제 조건** | TODO_ASSIGNED 알림 존재 |
| **테스트 절차** | 1. 알림 클릭 |
| **기대 결과** | 해당 할일 페이지(/todos)로 이동, 알림 읽음 처리 |
| **실제 결과** | - (프론트엔드 통합 테스트 시 검증 예정) |

### T-14. 토스트 팝업 동작

| 항목 | 내용 |
|------|------|
| **전제 조건** | SSE 연결 중, 새 알림 수신 |
| **테스트 절차** | 1. 다른 사용자가 담당자로 지정 |
| **기대 결과** | 화면 우측 상단에 토스트 팝업 (info 타입) 표시, 5초 후 자동 닫힘, 닫기 버튼 동작 |
| **실제 결과** | - (프론트엔드 통합 테스트 시 검증 예정) |

---

## 5. 엣지 케이스

### T-15. 미인증 사용자 접근 차단

| 항목 | 내용 |
|------|------|
| **테스트 절차** | 쿠키 없이 GET /api/v1/notifications 요청 |
| **기대 결과** | 401 Unauthorized 응답 |
| **실제 결과** | ✅ PASS - AuthGuard에 의해 차단 |

### T-16. 다른 엔티티(회사)의 알림 격리

| 항목 | 내용 |
|------|------|
| **테스트 절차** | 엔티티 A 소속 사용자가 엔티티 B의 알림을 조회 시도 |
| **기대 결과** | 빈 목록 반환 (ent_id 기반 필터링) |
| **실제 결과** | ✅ PASS - EntityGuard + ent_id 조건에 의해 격리 |

### T-17. 대량 알림 처리

| 항목 | 내용 |
|------|------|
| **테스트 절차** | 한 번에 10명의 참여자를 지정하여 할일 생성 |
| **기대 결과** | 10건의 알림이 각 참여자에게 개별 생성 (본인 제외 시 9건) |
| **실제 결과** | - (대량 데이터 테스트 시 검증 예정) |

### T-18. 오프라인 사용자 알림 보존

| 항목 | 내용 |
|------|------|
| **테스트 절차** | 사용자 B가 오프라인인 동안 담당자 지정 → 이후 로그인 |
| **기대 결과** | 로그인 후 미읽음 알림으로 확인 가능 (DB에 저장되어 있으므로) |
| **실제 결과** | ✅ PASS - DB에 ntf_is_read=false로 저장, 로그인 후 조회 가능 |

---

## 6. 테스트 요약

| 구분 | 총 건수 | PASS | 검증 예정 |
|------|---------|------|-----------|
| 단위 테스트 | 4 | 4 | 0 |
| API 테스트 | 4 | 4 | 0 |
| SSE 테스트 | 2 | 0 | 2 |
| UI 테스트 | 4 | 0 | 4 |
| 엣지 케이스 | 4 | 3 | 1 |
| **합계** | **18** | **11** | **7** |

> SSE/UI 테스트 검증 예정 항목은 스테이징 배포 후 멀티 사용자 환경에서 통합 테스트 진행 필요
