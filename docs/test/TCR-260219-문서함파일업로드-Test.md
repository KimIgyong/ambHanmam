# TC-문서함파일업로드-Test-20260219

## 기반 문서
- 요구사항 분석서: `docs/analysis/REQ-문서함파일업로드-20260219.md`
- 작업 계획서: `docs/plan/PLAN-문서함파일업로드-작업계획-20260219.md`

---

## 1. 단위 테스트 케이스

### TC-01: 업로드 버튼 표시 조건
| 항목 | 내용 |
|------|------|
| **전제조건** | 로그인 상태, Google Drive 설정 완료 |
| **테스트 단계** | 1. `/documents` 페이지 접속 → 루트 폴더 목록 확인 |
| **기대 결과** | 업로드 버튼이 **표시되지 않음** (루트 목록에서는 숨김) |

| 항목 | 내용 |
|------|------|
| **전제조건** | 동일 |
| **테스트 단계** | 2. 등록된 폴더 카드 클릭 → 폴더 내부 진입 |
| **기대 결과** | 업로드 버튼이 **표시됨** (헤더 영역) |

### TC-02: 단일 파일 업로드
| 항목 | 내용 |
|------|------|
| **전제조건** | 폴더 내부 진입 상태 |
| **테스트 단계** | 1. 업로드 버튼 클릭<br>2. 파일 선택 (예: test.pdf, 1MB)<br>3. 업로드 진행 확인 |
| **기대 결과** | - 업로드 진행 상태 표시 (spinner → 체크)<br>- 완료 후 파일 목록에 새 파일 표시<br>- 5초 후 진행 상태 자동 사라짐 |

### TC-03: 다중 파일 업로드
| 항목 | 내용 |
|------|------|
| **전제조건** | 폴더 내부 진입 상태 |
| **테스트 단계** | 1. 업로드 버튼 클릭<br>2. 파일 3개 동시 선택<br>3. 순차 업로드 확인 |
| **기대 결과** | - 3개 파일이 큐에 표시<br>- 순차적으로 업로드 (pending → uploading → done)<br>- 모든 파일 완료 후 목록 갱신 |

### TC-04: 파일 크기 제한 (50MB 초과)
| 항목 | 내용 |
|------|------|
| **전제조건** | 폴더 내부 진입 상태 |
| **테스트 단계** | 1. 업로드 버튼 클릭<br>2. 51MB 이상 파일 선택 |
| **기대 결과** | - 해당 파일 즉시 에러 상태 표시<br>- "50MB 이하 파일만 업로드 가능합니다." 메시지 표시 |

### TC-05: 네트워크 오류 처리
| 항목 | 내용 |
|------|------|
| **전제조건** | 폴더 내부 진입 상태 |
| **테스트 단계** | 1. 네트워크 끊김 상태에서 파일 업로드 시도 |
| **기대 결과** | - 업로드 실패 시 에러 아이콘(X) 표시<br>- 에러 메시지 표시 |

---

## 2. 통합 테스트 시나리오

### TC-06: 전체 업로드 플로우
| 항목 | 내용 |
|------|------|
| **전제조건** | admin@amoeba.group 로그인, ambCoWork 폴더 등록됨 |
| **테스트 단계** | 1. `/documents` 접속<br>2. ambCoWork 폴더 클릭<br>3. 업로드 버튼 클릭<br>4. 테스트 파일 선택<br>5. 업로드 완료 확인<br>6. Google Drive에서 해당 폴더에 파일 존재 확인 |
| **기대 결과** | - Google Drive에 파일 정상 업로드<br>- 파일 소유자: pm@amoeba.group (impersonated user)<br>- 문서함 목록에 즉시 반영 |

### TC-07: 하위 폴더 업로드
| 항목 | 내용 |
|------|------|
| **전제조건** | ambCoWork 내 하위 폴더 존재 |
| **테스트 단계** | 1. ambCoWork → 하위 폴더 진입<br>2. 해당 하위 폴더에 파일 업로드 |
| **기대 결과** | - 파일이 하위 폴더에 정상 업로드<br>- 브레드크럼 위치 유지 |

---

## 3. 엣지 케이스

### TC-08: 한글 파일명
| 항목 | 내용 |
|------|------|
| **테스트 단계** | "한글 파일명 테스트.xlsx" 파일 업로드 |
| **기대 결과** | 파일명이 정상적으로 Google Drive에 저장 및 표시 |

### TC-09: 특수문자 파일명
| 항목 | 내용 |
|------|------|
| **테스트 단계** | "file (1) [copy].pdf" 파일 업로드 |
| **기대 결과** | 파일명이 정상적으로 저장 및 표시 |

### TC-10: 빈 폴더에 업로드
| 항목 | 내용 |
|------|------|
| **전제조건** | 빈 폴더 진입 ("이 폴더에 파일이 없습니다." 표시 상태) |
| **테스트 단계** | 파일 업로드 |
| **기대 결과** | 업로드 완료 후 빈 폴더 메시지가 사라지고 파일 표시 |
